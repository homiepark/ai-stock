# 다음 세션 시작 시 복붙해서 보낼 메시지

> 이 파일 자체를 다음 세션에 첨부할 필요 없음. **아래 코드 블록의 내용만** 복사해서 새 채팅 첫 메시지로 붙여넣으세요.

---

## 📋 복사할 프롬프트

```
이전 세션에서 작업한 ai-stock 프로젝트인데 푸시가 막혀서 못 끝냈어. 다음 작업 부탁:

1. 로컬에 푸시 안 된 커밋 2개 있음:
   - 7f624b2: 과열 감지 + 레버리지 ETF + 초보자 UI + 코인 파이프라인 수정
   - cca5e6f: UI 폴리시 (hero 통계, 히트맵, 빅무버)

2. 먼저 git status / git log로 상태 확인하고
3. git push 시도해서 origin/main에 올려줘
   (이전 세션은 샌드박스 git 프록시 403 장애로 100번+ 시도해도 실패했음)

4. 푸시 성공하면 알려주고
5. GitHub Actions 워크플로 수동 실행 명령 알려줘 (코인 ID 수정이 적용되어 코인 데이터 살아나는지 확인 위해)

자세한 작업 내역은 docs/SESSION_HANDOFF.md 참조.

작업 디렉토리: /home/user/ai-stock
브랜치: main
```

---

## 📝 메모

위 프롬프트를 보내면 새 세션의 Claude가:
1. `git status`로 unpushed 커밋 확인
2. `git push` 시도 (새 샌드박스의 새 프록시 = 성공 확률 높음)
3. 성공 시 Vercel 자동 재배포 트리거됨
4. GitHub Actions 수동 트리거 안내

## 🆘 만약 새 세션도 푸시 실패하면?

매우 드물지만 가능. 그땐 본인 PowerShell에서 직접:

```powershell
cd C:\Users\remin\Documents\ai-stock
git pull
```

근데 이건 본인 측에 푸시할 commit이 없어서 의미 없음. 결국 누군가는 푸시해야 함. Claude 측 두 번째 세션도 안 되면 세 번째 새 세션을 열어보세요 — 거의 항상 그 안에 풀립니다.

## 🎯 이 작업이 끝나면 (Vercel 사이트에서 보일 것들)

- 코인 페이지 데이터 가득 (이전엔 비어있었음)
- 모든 종목 디테일에 "🎯 지금 사도 되나?" 박스
- 종합 매트릭스에 "과열도" 컬럼 (🟢🟡🟠🔴 + 0~100 점수)
- 종목 카드에 과열 배지
- 새 "레버리지" 테마 (TQQQ, SOXL 등 11종목, ⚠️ 고위험 표시)
- 홈 페이지 상단 "📖 초보자 가이드" 접힘 패널
- 매크로 지표(RSI 등) 위 마우스 오버시 툴팁
